apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev_infra_charts.fullname" . }}
  labels:
{{ include "dev_infra_charts.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "dev_infra_charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
