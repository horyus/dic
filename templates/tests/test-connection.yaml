apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev_infra_charts.fullname" . }}-test-connection"
  labels:
{{ include "dev_infra_charts.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dev_infra_charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
